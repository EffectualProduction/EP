<%- model_class = Usuario -%>
<div class="page-header">
  <h1 id="user">Usuarios</h1>
</div>
<div id="bus" class="page-header">
<%= form_tag request.path, :method => 'get' do %> 
 <%= content_tag :label do %> 
 <a id="nom">Usuario:</a> 
 <%= text_field_tag :search, params[:search] %> 
 <div id='botonc'>
 <%= submit_tag "Consultar", :name => nil, :id=>'botonc', :class => "btn btn-primary btn-small" %> 
 </div>
 <% end %> 
<% end %>  
</div>
<div id="te">
<div id="user" class="well" >
<table id="user" class="table table-striped table-bordered table-condensed">
  <thead>
    <tr>
      <th>id</th>
      <th>Nombre</th>
      <th>Apellido</th>
      <th>Email</th>
      <th>Fecha Naci</th>
      
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @usuarios.each do |usuario| %>
      <tr>
        <td><%= link_to usuario.id, usuario_path(usuario) %></td>
        <td><%= usuario.primnombre %></td>
        <td><%= usuario.primapellido %></td>
        <td><%= usuario.email %></td>
        <td><%= usuario.fecha_nac %></td>
        <td>
          <%= link_to image_tag('edi.png', :width=>30, :title=>'Editar'),edit_usuario_path(usuario), :class => 'btn btn-mini btn-inverse' %>
          <%= link_to image_tag('eliminar.png', :width=>30, :title=>'Eliminar'),
                      usuario_path(usuario),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
 </div>
<div id="nu">
<%= link_to image_tag('nuevo.png', :width=>'45px', :title=>'Nuevo'),new_usuario_path,:class => 'btn btn-inverse' %>
</div>

 <div id="text">
    <p align="justify">
      Esta Aplicación estará centrada en el Proceso  
    </p>
     <p align="justify">
       de control de Producción del Centro 
    </p>
     <p align="justify">
      de confección y moda (SENA), y por esta razón  
 </p>
  <p align="justify">
     constará de cuatro (4) Módulos, con
    </p>
    </div>
   
    <div id="pag"> 
 <%= will_paginate @usuario, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente
&raquo;' rescue nil %> 
</div> 
<img id="user" src="assets/user.png">