<%- model_class = Usuario -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:primapellido) %></th>
      <th><%= model_class.human_attribute_name(:secapellido) %></th>
      <th><%= model_class.human_attribute_name(:clave) %></th>
      <th><%= model_class.human_attribute_name(:email) %></th>
      <th><%= model_class.human_attribute_name(:fecha_nac) %></th>
      <th><%= model_class.human_attribute_name(:primnombre) %></th>
      <th><%= model_class.human_attribute_name(:secnombre) %></th>
      <th><%= model_class.human_attribute_name(:num_cedula) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @usuarios.each do |usuario| %>
      <tr>
        <td><%= link_to usuario.id, usuario_path(usuario) %></td>
        <td><%= usuario.primapellido %></td>
        <td><%= usuario.secapellido %></td>
        <td><%= usuario.clave %></td>
        <td><%= usuario.email %></td>
        <td><%= usuario.fecha_nac %></td>
        <td><%= usuario.primnombre %></td>
        <td><%= usuario.secnombre %></td>
        <td><%= usuario.num_cedula %></td>
        <td><%=l usuario.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_usuario_path(usuario), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      usuario_path(usuario),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_usuario_path,
            :class => 'btn btn-primary' %>
